---
- name: Converge
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    keda_wait_install: false
  tasks:
    - name: "Install KEDA"
      ansible.builtin.import_role:
        name: "keda"
