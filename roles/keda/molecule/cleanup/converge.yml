---
- name: Converge
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    keda_enabled: false
  tasks:
    - name: "Remove KEDA"
      ansible.builtin.import_role:
        name: "keda"
